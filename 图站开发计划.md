# 图站开发计划

> 更新日期：2026-08-06
> 本次更新：新增**视频展示功能**，内容形态扩展为「图集 + 单图 + 视频」三种

## 一、项目概述

一个**图片 + 视频展示网站**（图集 + 单图 + 视频三种内容形态混合），核心是**内容分级 + 付费解锁**：

- 运营方（管理员）在后台发布图片/视频内容，设置可见等级
- 不同等级用户看到不同内容（注册用户 -> V1 -> V2 -> V3）
- **未注册用户不可看到任何内容**，必须先通过邀请码注册并登录才能浏览
- 用户通过**卡密兑换**或**管理员手动发放**开通 VIP
- 仅在线浏览 / 在线播放，**不提供下载**，不做用户上传（UGC）
- 图片、视频全部接入 **Cloudflare R2** 对象存储，走 CDN 加速

## 二、需求整理

### 核心需求（用户已确认）

| 需求项 | 决策 |
|---|---|
| 内容形态 | 图集（相册）+ 单图 + 视频 三种类型混合 |
| 用户上传 | 不需要，纯展示站 |
| 内容分级 | 不同等级会员看到不同内容（图片、视频共用一套等级体系） |
| 付费方式 | 手动发放 VIP + 卡密兑换（不做自动支付） |
| 注册方式 | 邀请码注册 |
| 游客权限 | **不可见任何内容**，整站登录墙，需先注册并登录 |
| 下载功能 | 仅在线浏览 / 在线播放，不支持下载 |
| 技术栈 | PHP 8 + ThinkPHP 8 |
| 对象存储 | Cloudflare R2 |
| 部署 | 1Panel 面板 |

### 会员等级设计

| 等级值 | 身份 | 可看内容 |
|---|---|---|
| 0 | 注册用户（免费） | level 0 内容（注册即见，作为注册转化） |
| 1 | V1 | + level 1 内容 |
| 2 | V2 | + level 2 内容 |
| 3 | V3 | 全部内容（level 0 - 3） |

**判定规则**：
- **游客一律不可见任何内容**，仅可访问登录页、注册页、公告、关于页
- 注册用户 VIP 等级 >= 内容等级即可查看
- VIP 有有效期，到期自动降级
- **图片与视频共用同一 level 分级体系**；视频为独立内容类型，一个视频内容内不混排图片（见"视频内容形态"）

> 说明：原本的"公开内容层（游客可见）"已取消，改为整站登录墙。内容等级 level 0 保留作为"注册即可看的免费内容"，用于引导用户注册。

### 视频内容形态（本次新增）

| 决策项 | 方案 |
|---|---|
| 内容类型 | `albums.type` 扩展为 `album / single / video`，视频是**独立内容类型** |
| 文件形态 | 一个视频内容对应**一个视频文件**（MP4），浏览器原生 `<video>` 播放 |
| 处理方式 | **直传原始文件**，不做 ffmpeg 转码（服务器未装 ffmpeg） |
| 视频封面 | `poster` 由后台手动上传（复用图片上传流程），无 ffmpeg 无法自动抽帧 |
| 分级/登录墙 | 与图片一致：`level` 分级 + 整站登录墙 + VIP 到期降级 |
| 访问保护 | 仅登录后在线播放，不提供下载；隐藏下载控件、禁止右键保存 |
| 格式约束 | 后台限制上传 **MP4（H.264 + AAC）**，保证浏览器兼容与流式播放 |

## 三、技术栈选型

### 后端
- **PHP 8.4 + ThinkPHP 8**（服务器已有 PHP 8.4.6，环境零成本）
- **MySQL 8.4**（服务器已有）
- **Redis**（会话 / 缓存 / 限流，1Panel 一键安装；初期可用文件缓存过渡）
- **Composer** 管理依赖

### 前端
- **前台**：ThinkPHP 模板（服务端渲染，SEO 友好）+ **Tailwind CSS** + **Alpine.js**（灯箱、瀑布流等轻交互）+ **Plyr**（轻量 HTML5 视频播放器皮肤，移动端适配好）
- **后台**：**Vue 3 + Element Plus + Vite**，构建产物由 Nginx 托管，现代化管理界面

### 对象存储
- **Cloudflare R2**（S3 协议兼容），用官方 **AWS SDK for PHP**（aws/aws-sdk-php）对接
- R2 桶绑定自定义域名走 Cloudflare CDN，**免出口流量费**

### 图片处理
- **Imagick** 优先、**GD** 兜底：多尺寸缩略图 + **WebP** 压缩 + 水印

## 四、系统架构

```
浏览器
  |--> 前台页面 -----> Nginx/OpenResty -----> ThinkPHP 前台 (SSR 模板, SEO 友好)
  |--> 后台管理 -----> Nginx ---------------> /admin 静态资源 (Vue3 + Element Plus 打包产物)
  |--> 用户 API ------> Nginx ---------------> ThinkPHP api 路由 (JSON)
  |--> 图片/视频浏览 -> Cloudflare CDN ------> R2 桶 (图片直出 + 视频流式播放, 免流量费)
```

- 前台是服务端渲染，利于 SEO；用户相关操作用轻量 API
- 图片、视频文件不经过 PHP，由 R2 自定义域名 + CDN 直出
- 后台单独一个 Vue3 SPA，走 Token 鉴权

## 五、数据库设计（核心表）

- **users** 用户：id, username, email, password, salt, avatar, vip_level, vip_expire_at, status, invite_code_used, created_at
- **admin_users** 管理员：id, username, password, last_login_at, status
- **invite_codes** 邀请码：id, code(unique), status(unused/used/disabled), used_by, used_at, expire_at, created_at
- **card_batches** 卡密批次：id, name, level, duration_days, total, created_at
- **cards** 卡密：id, batch_id, code(unique), level, duration_days, status(unused/used/disabled), used_by, used_at, created_at
- **vip_logs** VIP 开通记录：id, user_id, level, duration_days, source(card/manual), card_id, remark, created_at
- **categories** 分类：id, name, slug, parent_id, sort, status
- **albums** 内容（图集/单图/视频）：id, title, subtitle, type(album/single/video), cover, level, category_id, status(draft/published/off), sort, view_count, like_count, created_at, updated_at
- **images** 图片：id, album_id, path, thumb_path, webp_path, sort, width, height, size, created_at
- **videos** 视频：id, album_id, path, poster, duration(秒), width, height, size, sort, created_at
- **tags** 标签：id, name, slug
- **album_tags** 图集标签关联：album_id, tag_id
- **favorites** 收藏：id, user_id, album_id, created_at（唯一约束 user+album）
- **browse_logs** 浏览记录：id, user_id, album_id, last_view_at
- **comments** 评论：id, album_id, user_id, content, status, created_at
- **settings** 系统设置：id, key(unique), value, remark
- **admin_logs** 后台操作日志：id, admin_id, action, target, ip, created_at

说明：单图、视频也复用 albums 表（type=single 只有一张图；type=video 对应 videos 表一条记录），后台管理统一。视频不新增等级字段，直接复用 albums.level；评论/收藏/浏览记录均以 album_id 关联，三种类型通用。

## 六、对象存储（Cloudflare R2）方案

- 桶结构：`albums/{album_id}/{sort}.jpg`，同一图集图片放同一目录；视频放 `albums/{album_id}/video.mp4`
- 每张图生成多尺寸：原图 / 封面图(约400px) / 列表图(约800px)，全部转 **WebP**
- 上传方式：后台通过 PHP（AWS SDK S3Client）签名上传；大图/视频可前端直传（预签名 URL + CORS）
- 展示域名：R2 桶绑定自定义域名（如 img.你的域名.com），Cloudflare CDN 直出
- 防盗链：Cloudflare 规则 + Referer 校验
- 水印：GD 生成时叠加，防搬运

### 视频存储与播放（本次新增）

- 视频文件统一推荐 **MP4（H.264 + AAC）**，保证浏览器原生播放；后台限制上传格式
- 播放依赖 R2/CDN 支持 **HTTP Range 请求**（拖动进度条、拖动预加载），Cloudflare 对 R2 默认支持
- 无 ffmpeg 转码：**poster 封面由后台手动上传**（复用图片上传流程），不自动抽帧
- 播放保护：登录后播放 + R2 **签名 URL**（预签名带有效期）+ Referer 校验；Plyr 隐藏下载控件、禁用右键保存
- 大视频上传：前端**预签名 URL 分片/直传**到 R2，避免 PHP 上传超时与内存占用

## 七、功能清单（含额外脑暴）

核心功能：
0. **登录墙**：未登录访问任何内容页/接口，跳转登录或注册页（前台中间件统一拦截）
1. 前台：首页瀑布流 / 内容详情（图集灯箱全屏浏览、单图查看、**视频播放页**）/ 分类页 / 标签页 / 搜索
2. 后台：内容管理（图集+单图+视频 CRUD、批量上传、排序、封面/视频封面）、分类标签管理、卡密管理、邀请码管理、VIP 手动发放、系统设置
3. 用户：邀请码注册、登录、个人中心（收藏、浏览记录、VIP 状态）

增值功能（可选分期）：
- 深色模式（前台适配）
- 图片水印 + 防盗链
- WebP 自适应压缩（AVIF 可选）
- SEO：伪静态、站点地图、结构化数据、详情页缓存（注：全站登录墙后搜索引擎无法收录内容页，SEO 价值有限，可仅做公开页的 SEO）
- 数据统计：访问量、图集热度榜、VIP 数据、卡密使用情况
- 站点公告 + 通知
- 评论审核（后台开关）
- 登录安全：验证码、登录限流、失败锁定
- 定时任务：VIP 到期自动降级、清理失效邀请码/卡密
- 分享预览：指定图集可生成限时分享链接（可设免登录预览，供站外引流）
- 视频播放统计 / 观看进度记录（可选）

## 八、项目目录结构

```
图站/
  app/
    index/      前台控制器（模板 SSR）
    api/        用户/前台 JSON 接口
    admin/      后台接口 + 登录鉴权
    common/     公共模型、服务、工具
  config/       TP8 配置（数据库、Redis、R2、上传）
  public/       入口 + 静态资源（前台 CSS/JS）
    admin/      Vue3 打包产物（由后台项目构建后放置）
  route/        路由定义
  database/     迁移脚本、填充数据
  admin-web/    后台前端源码（Vue3 + Element Plus）
  extend/       自定义类库
```

## 九、开发里程碑

- **M1 骨架搭建**（1天）：composer 创建 TP8、1Panel 建站 + 数据库建库、迁移建表、基础配置
- **M2 前台基础**（1-2天）：首页瀑布流、图集详情（灯箱）、单图查看、**视频播放页（Plyr）**、分类/标签/搜索（先用本地占位图）
- **M3 后台内容管理**（2-3天）：Vue3 后台骨架、登录、分类/标签/图集/图片/视频 CRUD、批量上传
- **M4 R2 存储接入**（1-2天）：AWS SDK、上传/删除、缩略图+WebP、CDN 域名、防盗链、**视频 Range 播放 + 大文件预签名直传**
- **M5 用户系统**（2天）：邀请码注册、登录、个人中心、内容分级过滤逻辑
- **M6 卡密与 VIP**（2天）：卡密批次生成/导出、兑换、手动发放、到期降级定时任务
- **M7 辅助功能**（2天）：收藏、浏览记录、统计、SEO、深色模式
- **M8 部署上线**（1天）：1Panel 正式站点、HTTPS、R2 正式桶、备份策略

合计约 2-3 周（业余时间）。

## 十、部署方案（1Panel）

1. 1Panel 创建 PHP 站点（OpenResty + PHP 8.4 已有容器）
2. 建 MySQL 数据库 + 专用账号
3. 安装 Redis（或先文件缓存）
4. 后台前端 build 产物放入 public/admin
5. Nginx 伪静态配置（ThinkPHP 需要）、/admin 静态代理
6. HTTPS 证书（1Panel 自动签发）
7. R2 桶创建 + 自定义域名 + CORS 配置
8. 定时任务：VIP 过期检测（cron 调 ThinkPHP 命令行）

## 十一、风险与注意

- 服务器 PHP 容器需确认已装 **gd/imagick** 扩展（没有则安装）
- R2 自定义域名需先在 Cloudflare 绑定域名并开代理（橙云）
- 备案问题：图站面向国内需 ICP 备案；R2/CDN 若用海外线路可暂不备案。因全站登录墙，内容页对搜索引擎不可见，建议保留公开的首页/公告页做站外引流与备案用途
- 内容合规：图片、视频内容需自审，避免违规
- 备份：MySQL 定时备份 + 图片/视频在 R2 自带版本管理
- **视频体量**：R2 免费额度约 10GB，视频占用量大需关注用量与计费；建议限制单视频大小（如 ≤500MB）并采用预签名直传
- **视频格式**：统一 MP4（H.264+AAC）；无 ffmpeg 转码，其他格式需后台自行转换后上传，否则浏览器不兼容
- **播放与防盗链**：需保证 CDN 缓存策略不影响 Range 流式播放；签名 URL 有效期与 Referer 校验需配合，防止视频被直接盗链下载

## 十二、下一步（M1 开启前需确认）

- 域名确认（主站 + R2 图片/视频子域名，可共用）
- R2 账号与 API Token（S3 凭证）准备
- 服务器 PHP 扩展检查（gd/imagick）
- 前台风格参考（给几张喜欢的设计样例）
- **视频源准备**：确认首期视频素材格式均为 MP4（H.264+AAC）、单文件大小上限（建议 ≤500MB）
